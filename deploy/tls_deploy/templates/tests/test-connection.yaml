apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "tls_deploy.fullname" . }}-test-connection"
  labels:
    {{- include "tls_deploy.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "tls_deploy.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
